import { fallbackPremiumCopy } from "./quiz_fallbacks";
export function scoreQuiz(quiz, answers = {}) { const scoreMap = {}; for (const a of quiz.result_archetypes || []) scoreMap[a.id] = 0; for (const question of quiz.questions || []) { const picked = answers[question.id]; const option = (question.options || []).find((opt) => opt.id === picked); if (!option) continue; for (const [id, weight] of Object.entries(option.archetype_weights || {})) scoreMap[id] = (scoreMap[id] || 0) + Number(weight || 0); } const sorted = Object.entries(scoreMap).sort((a, b) => b[1] - a[1]); const [topId = null, topScore = 0] = sorted[0] || []; const archetype = (quiz.result_archetypes || []).find((item) => item.id === topId) || quiz.result_archetypes?.[0]; return { score_map: scoreMap, primary_archetype: archetype?.label || "Unknown", primary_archetype_id: archetype?.id || null, preview: { headline: archetype?.headline || "Your pattern is clearer than it looks.", summary: archetype?.preview || "You finished the quiz, and a strong archetype emerged.", top_score: topScore }, premium: fallbackPremiumCopy(quiz, archetype?.label || "Primary Archetype", archetype?.preview || "") }; }
